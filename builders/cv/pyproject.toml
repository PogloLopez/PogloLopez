[project]
name = "cv-builder"
version = "0.1.0"
description = "Compila el CV de Pablo López (ES/EN) desde YAML + Typst a PDF"
requires-python = ">=3.11"
dependencies = [
    "pyyaml>=6.0",
]

[tool.uv]
package = false
